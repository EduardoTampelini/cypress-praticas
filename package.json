{
  "devDependencies": {
    "cypress": "^13.5.0",
    "mochawesome": "^7.1.3"
  },
  "scripts": {
    "cy:open": "cypress open",
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run": "cypress run"
  }
}
